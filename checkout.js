// let afterjustsomeminutes = document.querySelector("afterjustsomeminutes")
// let makeenoconfuseme = document.querySelectorAll("makeenoconfuseme")
// let currentIndex = 0
// let intervalTime = 5000

// function startCarousel() {
//     makeenoconfuseme[currentIndex].classList.add("active")
//     console.log(makeenoconfuseme)
// }

// Get the carousel container
// let carouselContainer = document.querySelector(".carousel-container");

// Get the carousel items
// let carouselItems = document.querySelectorAll(".carousel-item");

// Set the initial index
// let currentIndex = 0;

// Set the interval time (in milliseconds)
// let intervalTime = 5000;

// Set the interval ID variable
// let intervalID;


// Function to start the carousel
// function startCarousel() {
  // Set the initial active item
  // carouselItems[currentIndex].classList.add("active");

  // Set the interval to change the active item
  // intervalID = setInterval(function() {
    // Remove the active class from the current item
    // carouselItems[currentIndex].classList.remove("active");

    // Increment the index
    // currentIndex++;

    // Check if the index is greater than or equal to the number of items
    // if (currentIndex >= carouselItems.length) {
    //   currentIndex = 0;
    // }

    // Set the next item to active
    // carouselItems[currentIndex].classList.add("next");

    // Wait for the transition to complete, then remove the next class and set the new active item
//     setTimeout(function() {
//       carouselItems[currentIndex].classList.remove("next");
//       carouselItems[currentIndex].classList.add("active");
//     }, 500);
//   }, intervalTime);
// }

// Call the startCarousel function to begin the carousel
// startCarousel();
